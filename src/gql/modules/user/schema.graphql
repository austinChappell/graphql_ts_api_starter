type UserAll {
  data: [User!]
  fullCount: Int!
}

type User {
  email: String!
  firstName: String!
  id: ID!
  lastName: String!
  posts: [Post!]!
}

type Query {
  user(id: ID!): User!

  users(queryParams: QueryParams): UserAll!
}
